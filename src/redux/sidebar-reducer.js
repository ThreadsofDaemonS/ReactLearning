let initialState = {
  friends: [
    { id: 1, name: "Andrew" },
    { id: 2, name: "Sveta" },
    { id: 3, name: "Sasha" },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
